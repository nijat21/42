#include <unistd.h>

char **malloc(int rows)
{
    int max_length;
    char str[rows];

    max_length = 16;
    str = malloc()
}

void *ft_print_memory(void *addr, unsigned int size)
{
    char *str;
    int i;

    if (size == 0)
        return (0);
    while (str[i])
    {
        if (!(32 <= str[i] && str[i] <= 126))
            str[i] = ".";
    }

    return addr;
}

int main(void)
{
    char **str =
        return (0);
}